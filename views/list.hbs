<h4>List</h4>

{{#if user}}
Welcome {{user.username}}
  <form action="/logout" method="POST" id="form">
    <button class="pull-right logout" type="submit">Log Out</button>
  </form>

{{else}}
  <a class="pull-right signup" href="/signup">Sign up</a>

  <a class="pull-right login" href="/login">Log In</a>
{{/if}}

<br>
<br>

<a href="/add">Add New</a>

<br>
<br>
<br>

<h4>Select:</h4>

<form class="list-forms" action="/list" method="GET">
  <button type="submit">All</button>
</form>

<form class="list-forms" action="/list" method="GET">
  <label for="">Machine</label>
 <select name="machine" id="machine">
    <option>Choose a machine</option>
      {{#each machines}}
        <option value="{{this._id}}">{{this.machine}}</option>
      {{/each}}
  </select>
  <button type="submit">Submit</button>
</form>

<br>
<br>
<br>
  

<table class="grid">
  <tr>
    <th class="th">Machine</th>
    <th class="th">Date</th>
    <th class="th">Synthesis</th>
  </tr>
  <tr>
    <td class="td">{{#each logs}}
    <a>{{this.machine.machine}}</a><br>
      {{/each}}</td>
    <td class="td">{{#each logs}}
    <a>{{dateFormat this.date "DD/MM/YYYY"}}</a><br>
      {{/each}}</td>
    <td class="td">{{#each logs}}
    <a href="/details/{{this._id}}">{{this.synthesis}} </a><br>
      {{/each}}</td>
      </tr>
</table>

<div class="weather">
<div class="api">
  <div id="wicon"><img id="wicon" src="http://openweathermap.org/img/wn/{{weatherIcon}}@2x.png" alt="Weather icon"></div>
</div>
 <div class="api"> 
  <div class="temp">{{temperature}}ºC</div>
</div>
</div>
 